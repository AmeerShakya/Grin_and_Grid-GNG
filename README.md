# Grin_and_Grid-GNG

Grin and Grid is a Project explicitly adapted to:  Jackbox-style gameplay with separated backend & frontend for Host + Player authentication where XAMPP + MySQL (MariaDB) is used as the database stack and Django as backend authority.
Grin → Characters, personality, roleplay, choices
Grid → Tactical combat, positioning, rules, math
